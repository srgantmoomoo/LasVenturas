package me.srgantmoomoo.lasventuras.client.ui;

import java.awt.Color;

import me.srgantmoomoo.lasventuras.api.event.customevent.listeners.EventRenderGUI;
import me.srgantmoomoo.lasventuras.client.LasVenturas;
import me.srgantmoomoo.lasventuras.client.module.Module;
import me.srgantmoomoo.lasventuras.client.module.ModuleManager;
import net.minecraft.client.Minecraft;
import net.minecraft.client.gui.FontRenderer;
import net.minecraft.client.gui.GuiScreen;
import net.minecraft.client.gui.ScaledResolution;

public class UIRenderer extends GuiScreen {
	private Minecraft mc = Minecraft.getMinecraft();

	public void draw() {
		final int[] counter = {1};
		ScaledResolution sr = new ScaledResolution(mc);
		FontRenderer fr = mc.fontRendererObj;
		
		fr.drawStringWithShadow("L", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("a", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("s", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("V", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("e", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("n", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("t", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("u", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("r", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("a", 0, 0, rainbow(counter[0] * -300));
		fr.drawStringWithShadow("s", 0, 0, rainbow(counter[0] * -300));
		counter[0]++;
	}
	
	public static int rainbow(int delay) {
		double rainbowState = Math.ceil((System.currentTimeMillis() + delay) / 20.0);
		rainbowState %= 360;
		return Color.getHSBColor((float) (rainbowState / 360.0f), 0.5f, 1f).getRGB();
	}
}